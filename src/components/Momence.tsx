"use client"

import { typography, spacing } from "@/styles/design-tokens";
import type {Momence as MomenceProps} from "@types"

export default function Momence({
  title,
  integration,
}: MomenceProps) {
  return (
    <section>
      <div>
        <p>Momence Section</p>
      </div>
    </section>
  );
}
